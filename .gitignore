# ESP-IDF build output
/build/
sdkconfig
sdkconfig.old
/managed_components/
dependencies.lock

# host test artifacts
test/host/bin/

# misc
.DS_Store
*.o
*.log
test/host/out/

# hardening: never commit the Secure Boot signing key
secure_boot_signing_key.pem
sdkconfig.hardening

# archived images, kept so a core dump can be decoded against what was flashed
.flashed/
.preview/
